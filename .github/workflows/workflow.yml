name: CI/CD pipeline for main branch

on:
  push:
    branches:
      - main

env:
  PYTHONPATH: api/src/

jobs:
  test_api:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        cd api/
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Install dev dependencies
      run: |
        cd api/
        pip install -r requirements-dev.txt
    - name: Test with pytest
      run: pytest
    - name: Code analysis
      run: |
        cd api/
        flake8 src

  test_cdk:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Use Node.js 10.x
      uses: actions/setup-node@v1
      with:
        version: 10.x
    - name: Install dependencies
      run: |
        cd infrastructure/
        npm install
    - name: CDK unit tests
      run: |
        cd infrastructure/
        npm test  

# TODO - test Cypress

# TODO - deploy infrastructure using CDK

# TODO - build React app and copy files to S3 bucket for static website

# TODO - e2e tests
